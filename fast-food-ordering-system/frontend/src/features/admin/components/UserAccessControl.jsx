/**
 * Owner-only staff account management (create cashier/kitchen/admin
 * accounts, set PINs, deactivate). Needs a GET/POST /api/users backend
 * route pair, which is intentionally out of scope for Phase 1 (see
 * PHASE 1 FOCUS in the project brief) - this stub establishes where that
 * screen will live once those endpoints exist.
 */
export default function UserAccessControl() {
  return (
    <div style={{ padding: '1rem' }}>
      <h2>Access Control</h2>
      <p>Staff account management coming soon.</p>
    </div>
  );
}
